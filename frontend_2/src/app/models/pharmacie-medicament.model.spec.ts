import { PharmacieMedicament } from './pharmacie-medicament.model';

describe('PharmacieMedicament', () => {
  it('should create an instance', () => {
    expect(new PharmacieMedicament()).toBeTruthy();
  });
});
